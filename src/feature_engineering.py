import pandas as pd

def feature_engineering(df: pd.DataFrame) -> pd.DataFrame:
    """
    Baseline feature engineering.
    For Milestone 1 you can keep it minimal.
    Later you can add new features like:
    - log transforms
    - ratios
    - interaction terms
    """
    df = df.copy()
    return df
